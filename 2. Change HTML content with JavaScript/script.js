function changeText() {
    // Targeting the HTML element id.
    let heading = document.getElementById("text");
    // Asigning new text to the HTML element 
    heading.textContent = "New text";
};
